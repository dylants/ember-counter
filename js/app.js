/**
 * Defines the CounterApp, an Ember Application
 * 
 * @type {Ember.Application}
 */
window.CounterApp = Ember.Application.create({
	// debug needed to log all transitions to the console
	LOG_TRANSITIONS: true
});